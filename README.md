# Codegen

The following will help you generate the latest schema changes and generate interface files to be used in the backend end code.

## Schema

1. Run Server
2. Run `npm run codegen` this will generate interfaces files based on graphql schema
